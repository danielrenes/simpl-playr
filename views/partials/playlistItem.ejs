<% include ../helpers/formatter %>

<tr class="playlist-item">
    <th><%= song.title %></th>
    <td><%= song.artist %></td>
    <td><%= song.album %></td>
    <td><%= formatTime(song.duration) %></td>
</tr>